#include <stdio.h>
#include <stdlib.h>
#include <ft_pipex.h>
#include <ft_base_array.h>

int main1(void)
{
	char str[BUFFER_SIZE];
	scanf("%[^\n]", str);
	//int	i = -1;
	//ft_divide_cmd(str);
	return (0);
}
